/**
 * CLI utility constants
 */

/**
 * Fallback terminal width (columns) when `process.stdout.columns` is unavailable.
 * 80 = classic POSIX default (predates wide terminals).
 */
export const DEFAULT_TERMINAL_WIDTH = 80;
